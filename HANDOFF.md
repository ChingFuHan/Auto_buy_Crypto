## HANDOFF - BINANCE_SMALLCAP_FIRST_PUMP_ENTRY_STOP_SYSTEM 熱區

更新時間：2026-05-11 03:08 +08:00
整理快照：`git HEAD=a131187`（client id 修復已提交）

### 本次完成事項
- 2026-05-11（live 執行前交接）：使用者表示「那我執行 live bot 囉」。本輪回覆：由使用者自行執行，Agent 不啟動、不停止、不重啟 bot；執行前建議確認沒有舊 `main.py run` 進程，並確認目前程式在 `a131187 fix: sanitize Binance client order ids` 或其後續 commit。`git status --short` 查詢當下乾淨，`git rev-parse --short HEAD` 為 `a131187`。未改 `.env`、未連 Binance、未查帳戶、未查/寫 DB。
- 2026-05-11（本輪提交基準後修復 Binance client order id）：使用者要求「先提交變更，再解 bug」。已先將既有工作區變更提交為 `8c5cda7 chore: save current trading bot updates`。隨後修復 `newClientOrderId/clientAlgoId` 非 ASCII 問題：新增 `pump_system/utils/client_order_id.py`，以 Binance regex `^[.A-Z:/a-z0-9_-]{1,36}$` 產生安全 id；entry、native stop、fallback close 均改用此 builder。真正送 Binance 的交易 `symbol` 保持原值，例如 `币安人生USDT` 不被改名；只把 client id 轉成類似 `entry_usdt_<hash>_...` / `stop_usdt_<hash>_...` / `fallback_usdt_<hash>_...`。新增回歸測試覆蓋中文 symbol 的 entry id、stop `clientAlgoId`、fallback close `newClientOrderId`。驗證：`python3 -m py_compile pump_system/utils/client_order_id.py pump_system/execution/order_service.py pump_system/fallback_stop/manager.py tests/test_order_service_stop.py tests/test_fallback_stop_manager.py` 通過；`python3 -m pytest -q tests/test_order_service_stop.py tests/test_fallback_stop_manager.py` 得 `13 passed`；`python3 -m pytest -q` 得 `53 passed, 1 xfailed`。本輪未改 `.env`、未連 Binance API、未查帳戶、未查/寫 DB、未啟動/停止/重啟 bot、未改訊號條件或 sizing。
- 2026-05-10（本輪 `ENTRY_ORDER_FAILED newclientorderid` 只讀排查）：使用者貼出 live Telegram 連續 `ENTRY_ORDER_FAILED`，symbol=`币安人生USDT`，Binance 回 `status=400 code=-1100`，指出 `newclientorderid` 僅允許 `^[.A-Z:/a-z0-9_-]{1,36}$`。本輪依序讀 `god_rule.md`、`README.md`、`HANDOFF.md`，再只讀查 `pump_system/execution/order_service.py`、`pump_system/fallback_stop/manager.py`、`pump_system/exchange/symbol_registry.py`、`pump_system/exchange/binance_client.py`、`tests/test_order_service_stop.py` 與 `logs/app.log.5`。結論：`_build_entry_client_order_id()` 目前只處理長度，未把 symbol 做 ASCII 白名單清洗；對中文 symbol 會產生類似 `entry_币安人生usdt_...` 的 `newClientOrderId`，因此被 Binance 拒單。`logs/app.log.5` 於 2026-05-10 22:29:22 至 22:30:34 共 73 筆同類 `newclientorderid` 錯誤；同一 bar 內連續重試的次因是 `_last_handled_bar` 只在 entry 成功後才設定，entry 失敗會讓後續 market update 再次嘗試。另發現 stop `clientAlgoId` 與 fallback close `newClientOrderId` 也直接拼 `symbol.lower()`，遇到非 ASCII symbol 時有同類風險。本輪未改交易邏輯、未改 `.env`、未連 Binance API、未查帳戶、未查/寫 DB、未啟動/停止/重啟 bot、未跑測試。
- 2026-05-10（本輪 v1 read-only income 工具實作）：使用者貼上 v1 執行規格，要求新增 Binance USD-M Futures 近 N days 帳戶 income read-only 工具。本輪先讀 `AGENTS.md`、`god_rule.md`、`README.md`、`HANDOFF.md`；確認規格限制交付檔案與 `god_rule.md` RULE 01 存檔義務有衝突，依上位規則額外更新 `HANDOFF.md`。已新增 `tools/binance_usdm_income_readonly.py`、`tools/binance_usdm_income_readonly_GUIDE.md`，並在 `.gitignore` 末尾追加 `tools/outputs/`。工具 v1 僅支援 `COMMISSION` 與 `FUNDING_FEE`，預設 dry-run，execute 需 `--execute --confirm-readonly`，只 allowlist `GET /fapi/v1/time` 與 `GET /fapi/v1/income`，報表只寫入 `tools/outputs/`。驗證：`python3 -m py_compile tools/binance_usdm_income_readonly.py` 通過；`python3 tools/binance_usdm_income_readonly.py --help` 通過；使用 `/tmp/binance_income_fake.env` dry-run 通過，輸出 `WILL_SEND_BINANCE_REQUEST: NO`、`request_count: 0`。未讀真實 `.env`、未發 Binance request、未查帳戶、未寫 DB、未啟動/停止/重啟 bot、未改交易邏輯。dry-run 偵測到疑似 `main.py run`，工具只輸出 WARNING，未操作該進程。
- 2026-05-10（本輪 prompt review）：使用者貼出 Claude Code 對 Binance USD-M income read-only 工具執行 prompt 的 review，要求本輪完整理解專案脈絡後只做 prompt review、不要執行 prompt 內任務。本輪已讀 `god_rule.md`、`README.md`、`HANDOFF.md`、`AGENTS.md`，並補核 Binance 官方 `/fapi/v1/income` 文件與 API-Agent futures rebate 文件。結論：prompt 大方向可用，但需補強「可見 prompt 不完整限制」、`.env` 預設與 fake env 驗證衝突、final 回報/HANDOFF 不得寫金額與 report 可寫金額的範圍衝突、fingerprint 禁止輸出與 fingerprint function 的衝突、lead trader/帶單員語意與 endpoint allowlist、`/fapi/v1/income` 3 個月限制、retry 對 `-1003`/429/418/5xx 的處理、`.gitignore` 重複追加與完成回報重複段落。未執行 prompt、未讀 `.env`、未建立工具檔、未啟動/重啟 bot、未連 Binance 帳戶/API、未查帳戶、未查/寫 DB。
- 2026-05-10（本輪修正版完整 prompt 交付）：使用者要求「在這邊給我完整版的 prompt」。本輪將 Claude 版最終 prompt 與上一輪核對出的 4 個修正整合成可直接貼給實作 agent 的完整版本：改用 `/fapi/v1/income` 官方 `page`/`limit` 分頁、工具內部 `[start,end)` 但 request `endTime=end_ms-1`、補 `label` regex、移除 prompt 外 meta 語句。未執行 prompt、未讀 `.env`、未建立工具檔、未連 Binance。
- 2026-05-10（本輪 Claude 最終版 prompt 核對）：使用者貼出 Claude Code Opus 4.7 產出的最終版 prompt 與其 HANDOFF 寫入內容。本輪只做結果核對，未執行 prompt。結論：Claude 版大方向可用，且比前版補上 time-sync 隔離、`dotenv_values()`、error code retry、leak gate、`tools/outputs/`、profile regex 等重要保護；但交給實作 agent 前仍需修正：`/fapi/v1/income` 官方支援 `page`/`limit` 分頁，Claude 版要求「不得用 page、用 last_record time + 1」可能在同 timestamp 多筆資料時跳漏；工具內部可維持 `[start, end)`，但送 Binance `endTime` 前需轉成 `end_ms - 1` 或明確標註 request 用 inclusive end；另需刪掉 prompt 外殘留的「更 HANDOFF 一條」meta 語句，並補上 label regex。
- 2026-05-10（本輪 Claude Code 接手最終 prompt 交付）：使用者帶來前序兩段外部 agent QA 完整 transcript，要求重新審視整體 context 後產出最終版執行規格 prompt。本輪以 Opus 4.7 max effort 重新讀 `god_rule.md` / `README.md` / `HANDOFF.md` / `AGENTS.md` / `config.py` / `.env.example` / `.gitignore` / `requirements.txt` / `pump_system/exchange/binance_client.py` 片段，並比對 Binance `/fapi/v1/income` 行為。相對於上一輪 gpt-5.5 交付的補強：明確 Python ≥ 3.9 + `zoneinfo`、用 `dotenv_values()` 不污染 `os.environ`、live bot 偵測加入 `live_run.pid` 與 `pgrep -f 'main\.py run'` + `kill -0`、retry 政策依 Binance error code 分流（`-1021` 重 sync time 後 retry 1 次，`-2014`/`-2015` 不 retry 直接 BLOCKED 該 profile，429/418/5xx/network 才 retry 1 次）、secret leak gate 列出 5 條偵測 OR 規則（完整 api_key 字串/`X-MBX-APIKEY`/`signature=` hex/長 base64/`.env` 整行）、`--handoff-path` 預設改為 `tools/outputs/HANDOFF_binance_usdm_income.md` 並禁止指向專案 `HANDOFF.md`、`--output-dir` 預設 `tools/outputs/`（含 `.gitignore` 追加）、報告含 `tran_id` 欄與 grand total across profiles、income type allowlist 限 `COMMISSION`/`FUNDING_FEE`、禁止 import 主專案 `BinanceClient` 並要求自帶 hmac-sha256 與獨立 `/fapi/v1/time` sync、明確 `[start, end)` 半開區間語意、profile schema 明確 regex 限制。本輪未執行 prompt、未讀 `.env`、未連 Binance、未建立工具檔。
- 2026-05-10（本輪最終 prompt 交付）：使用者要求「給我修改後的完整 prompt」。本輪只整理並交付一份可直接貼給下一個 agent 的最終執行規格 prompt，將前序補強壓縮為命令式規格，重點包含：三個工具檔交付、profiles example/local 分離、`--profiles-file` 不預設讀 example、只支援 active source、UTC+8 固定時間格式、官方 base URL allowlist、預設 dry-run、fake env 驗證、report 為主輸出、JSON/CSV 選配、overwrite 預設 blocked、Handoff append-only + secret gate、原始 signed amount 保留、禁止 live bot 干擾與任何寫入型 Binance endpoint。
- 2026-05-10（本輪 prompt review 續）：使用者貼出 10 點補強段落，並新增要求「省略廢話、簡潔有力、不要讓 agent 注意力不集中」。本輪只做 prompt review，未執行工具實作。結論：補強方向正確，但目前文字仍像「修改建議清單」而非最終執行規格；建議再壓縮成 imperative spec，移除「建議/原本/額外提醒」等 meta 語句，並補強幾個會誤導實作的點：`--profiles-file` 不應預設自動載入 example、目前只支援 active source、local profiles 應加入 `.gitignore`、`funding rate` 應更名為 `funding fee amount` 除非另開 scope 查 rate、report 作主輸出、JSON/CSV 作選配。
- 2026-05-10（本輪 prompt 補強決策）：使用者針對上一輪 10 點 review 建議逐項給出採納方向。關鍵決策：時間輸入堅持使用預設 UTC+8 的 `YYYY-MM-DD HH:MM:SS`；key profile 不再直接從 `.env` 內註解行解析，而改為另外建立可擴充的 key profile 設定檔供 Python 讀取；輸出需要一個人類可讀細節檔，列出期間內每個幣種的手續費與 funding fee/funding rate 相關金額，包含時間、幣種、手續費，檔案末尾要有 total fee；原始金額正負號必須保留，不得取絕對值或任意翻號。其餘安全建議採納：官方 base URL allowlist、fake env 驗證、輸出檔避免覆蓋、Handoff append-only + secret gate、request weight 預估等。
- 2026-05-10（本輪 prompt review）：使用者貼出新版「Binance USD-M Futures read-only income 查詢工具」實作 prompt，明確要求只做 prompt review、不執行 prompt。本輪已按進場順序讀 `god_rule.md`、`README.md`、`HANDOFF.md`、`AGENTS.md`，並核對既有 `config.py` base URL 邏輯與 Binance 官方 `/fapi/v1/income` 文件。結論：prompt 整體已足夠接近可執行，但仍建議補強 8 點：簡化模式必填與 fallback 規則衝突、時間格式契約矛盾、`--base-url` 需官方 allowlist、`.env` commented parser 語法需定義、實作驗證應使用 fake env 避免讀真實 `.env`、輸出檔 overwrite 規則、CSV 多表結構、Handoff 寫入 append-only 與 secret leak gate。
- 2026-05-10（本輪 prompt 補強交付）：使用者補充「`.env` 為主，`.env.example` 直接忽略」，要求提供補強後完整版 prompt。本輪只依前序 review 結論整理可直接交給 agent 的 read-only Binance fee/funding 查詢 prompt，明確要求以實際 `.env` 為唯一設定來源、不得讀 `.env.example` 作為 API key 結構前提、不得輸出 secrets、不得修改 `.env`、不得啟動/重啟 live bot、不得下單/取消單/改槓桿/寫 DB/寫交易所 API。
- 2026-05-10（本輪 prompt/meta-review）：使用者貼出兩段 agent QA，要求只看 QA 做分析；本輪依 review 邊界處理，未執行被審查 prompt。已讀 `god_rule.md`、`README.md`、`HANDOFF.md`、`AGENTS.md`、`.env.example` 與最小相關程式碼，並核對 Binance 官方文件。確認 `.env.example` 只有一組 `API_KEY/API_SECRET`；「兩組 key（一個註解、一個 live）」不是公開範例支撐的既定事實。結論方向：外部 meta-review 對 secret masking、低干擾、輸出格式、時間範圍多數判斷合理，但「`/fapi/v1/income` 單次最多只能查 7 天」需修正；目前官方文件將 7 天限制明確寫在 `/fapi/v1/userTrades` 時間區間，`/fapi/v1/income` 則是未帶時間預設最近 7 天且支援 `page/limit`。
- 2026-05-09（本輪修復）：已把 `restore_native_stop_watchlist()` 的誤判條件修掉。現在不再依賴 `clientAlgoId.startswith("stop_")`，而是以 `orderType=STOP_MARKET`、`positionSide=LONG`、`side=SELL`，並且 `closePosition=true` 或 `reduceOnly=true` 來判定是否為 native protective stop。對應回歸測試已同步更新為接受實際 Binance 回傳的 `ct-ldr-...` 類 clientAlgoId。驗證：`py_compile` 通過；`python3 -m pytest -q tests/test_algo_fill_regression.py tests/test_order_service_stop.py` 得 `16 passed, 1 xfailed`；`python3 -m pytest -q` 得 `48 passed, 1 xfailed`。
- 2026-05-09（本輪新訊息）：使用者貼出最新 Telegram。判讀：`POSITION_WITHOUT_STOP_DETECTED` 仍在 bootstrap/restore 期集中出現，但後段新增 `WEBSOCKET_RECONNECTING`、`WEBSOCKET_CATCHUP_COMPLETED`、`SERVER_TIME_OFFSET_BLOCKED`、`BINANCE_API_BLOCKED`。其中 `SERVER_TIME_OFFSET_BLOCKED` 已多次超出門檻，例如 `8084`、`12698`、`92826`、`109661` ms，代表 live 簽名請求與 `/fapi/v2/positionRisk`、`/fapi/v1/openAlgoOrders` 已進入不健康狀態；這不是單純的 restore 誤報，而是目前 live 環境仍有 time-sync / websocket instability。
- 2026-05-09（本輪判讀）：OpenCode Zen 的根因分析大致正確。`restore_native_stop_watchlist()` 的確在 `orderType=STOP_MARKET`、`positionSide=LONG` 之後又多卡了一層 `clientAlgoId.startswith("stop_")`，而本輪 read-only 查到的現存 protective stop id 格式不符合這個前綴，因此 bootstrap 會一直誤報 `POSITION_WITHOUT_STOP_DETECTED`。不過修復建議要稍微修正：不要把 `closePosition=true` 當成已驗證前提直接寫死，應先以「同 symbol 的 `STOP_MARKET + LONG` conditional order 即視為 attached」作為最小修正，再視 Binance 回傳欄位決定是否把 `closePosition` / `reduceOnly` 當成額外保護條件。
- 2026-05-09（本輪回應）：使用者詢問 `POSITION_WITHOUT_STOP_DETECTED` 是否會一直存在。已確認這不是週期性背景任務，而是 `TradingApplication._bootstrap()` 在 `main.py run` 開機時呼叫 `restore_native_stop_watchlist()` 進行一次 restore 檢查；因此只要這批持倉仍開著、且程式仍用現行判定條件（`STOP_MARKET` / `positionSide=LONG` / `clientAlgoId` 需以 `stop_` 開頭）無法辨識它們，則每次啟動/重啟都會再次出現相同告警。若不重啟，這批 restore 告警通常不會持續週期性重刷；但只要日後因 crash / 手動重啟再次進入 bootstrap，仍會重現。
- 2026-05-09（本輪觀察續行）：`python3 main.py run` 已成功進入 live；startup log 已送出 `APP_STARTUP_SUCCESS` / `MODE_SUMMARY` / `LIVE_PRODUCTION_MODE` / `SERVER_TIME_SYNC_OK`。但 restore 階段再次對多個 symbols 送出 `POSITION_WITHOUT_STOP_DETECTED`，原因已定位為程式 `restore_native_stop_watchlist()` 只承認 `clientAlgoId` 以 `stop_` 開頭的 `CONDITIONAL STOP_MARKET`，而本輪 read-only 查到的現存 protective stop 其 id 格式並不符合此條件，因此 bot 目前仍會把它們視為「沒保護單」並持續告警。
- 2026-05-09（本輪觀察續行）：`python3 main.py backfill` 已完成後續等待並切換進 `python3 main.py run`。`backfill` 中途曾因 `/fapi/v1/klines` 遇到 `429` rate limit 而等待 60 秒，之後 `run` 已成功啟動，目前觀察到大量 `staging updated` / `signal check` 正常輸出，尚未見新的 blocked error。
- 2026-05-09（本輪觀察續行）：`python3 main.py backfill` 已進行中，log 顯示目前已遇到 Binance REST `429` rate limit，`/fapi/v1/klines` 觸發 `Too many requests` 後進入 `wait 60.0 sec until next minute`。這表示 backfill 仍在跑，但會因交易所速率限制延後進度；尚未進入 `run`。
- 2026-05-09（本輪觀察續行）：使用者本機 sequential 命令已進入 `python3 main.py backfill`；前一段 `python3 main.py validate` 已結束，log 顯示其在 restore 階段再次打出 `POSITION_WITHOUT_STOP_DETECTED`，之後結束於 `validation bootstrap completed`。目前觀察重點切到 backfill 是否能順利完成，以及後續是否進入 `run` 並持續穩定。
- 2026-05-09（本輪收尾）：使用者明確表示將直接執行 `python3 main.py validate`、`python3 main.py backfill`、`python3 main.py run`。目前 bot 不在執行中；本輪已完成的 audit retention/compression 修補與 Binance read-only 查詢都已寫入 handoff，接下來由使用者在本機跑 live 前置流程。
- 2026-05-09（本輪續行）：完成 audit retention/compression 程式修補，未手動刪除/壓縮/搬移既有 `data/audit/signal_decisions/*.jsonl`。新增/更新：`config.py` audit 維護設定、`.env.example` 範例、`pump_system/audit/jsonl_parquet.py` gzip/parquet 維護器、`SignalDecisionAuditWriter.run_maintenance()`、`TradingApplication` 背景維護 loop、README 說明、測試。預設行為：每 3600 秒維護一次，只壓縮跨日 JSONL 為 `.jsonl.gz`，成功寫出 `.tmp` 並原子替換後才刪原 `.jsonl`；`SIGNAL_AUDIT_RETENTION_DAYS=7` 只刪已封存舊檔，未封存 JSONL 不直接刪。另將 `data/audit/` 與 `data/*.tmp` 加入 `.gitignore`，避免 runtime 輸出誤入版本控制。
- 2026-05-09（本輪續行）：驗證完成：`PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile config.py pump_system/audit/jsonl_parquet.py pump_system/audit/signal_decision_audit.py pump_system/app.py tests/test_signal_decision_audit.py tests/test_app_heartbeat.py` 通過；`python3 -m pytest -q tests/test_signal_decision_audit.py tests/test_app_heartbeat.py` 結果 `12 passed`；全測試 `python3 -m pytest -q` 結果 `48 passed, 1 xfailed`。
- 2026-05-09（本輪續行）：使用者回覆「1 已停止、2 先保留、3 你幫我做、4 授權給你」，本輪解讀為：使用者已停止 live bot；`data/audit/signal_decisions/*.jsonl` 暫不刪除/搬移；授權 Agent 做 audit retention/compression 程式修補；授權 Agent 只讀查 Binance 帳戶現況。`pgrep -af 'python3 main.py run|main.py run'` 只看到查詢命令本身，確認沒有 `python3 main.py run` 持續執行。
- 2026-05-09（本輪續行）：已用現有 `.env` 只讀查 Binance Futures 正式盤 GET 端點（未送 order/cancel/leverage/margin POST）。結果：`TESTNET=false`、`ENABLE_LIVE_TRADING=true`、server time offset 約 `+49ms`；帳上共有 43 個 open positions、0 個 regular open orders、43 個 conditional algo orders。先前 31 個 restore 告警 symbols 目前全都有 1 張 `STOP_MARKET SELL` protective conditional algo stop；全部 43 個 open positions 也都各有 1 張 protective stop，`missing_protective_stop=NONE`。
- 2026-05-09（本輪）：本機唯讀 triage 確認：目前掛載點 `/media/sf_agent_sanbox_vm` 仍為 `931G/907G used/24G avail`，使用率 98%；專案本身約 8.7G，其中 `data/audit/signal_decisions/` 佔 8.5G，`signal_decisions_15m_20260508.jsonl` 為 7.9G、`signal_decisions_15m_20260509.jsonl` 約 620M+ 且仍在增長。`logs/` 約 128M，不是本專案內最大來源。
- 2026-05-09（本輪）：本機 log 顯示 15:50、15:51、15:52 多次 restore：`fallback.stop restored fallback stops active=0`，隨後 `execution.order` 對 31 個 positions 打出 `[CRITICAL] position without protective stop detected on restore`，包含使用者貼出的 11 個 symbols，另有 `DEXEUSDT`、`TRXUSDT`、`PLUMEUSDT`、`ZECUSDT`、`STXUSDT`、`TRUMPUSDT`、`PUMPUSDT`、`AXLUSDT`、`TAKEUSDT`、`REDUSDT`、`ONUSDT`、`RSRUSDT`、`EGLDUSDT`、`TONUSDT`、`JUPUSDT`、`INXUSDT`、`YGGUSDT`、`KITEUSDT`、`MYXUSDT`、`AZTECUSDT`。目前只確認本地 log，未查交易所帳戶現況。
- 2026-05-09（本輪）：程式碼確認 `OrderService.restore_native_stop_watchlist()` 在 restore 時只會：查 open algo stop、比對 `stop_` client id 與 fallback active record；若都沒有，只發 `POSITION_WITHOUT_STOP_DETECTED` 告警，不會自動補掛 stop 或平倉。`PositionState` 只保留 `positionAmt > 0`，Telegram 的 `side=SELL` 是平倉保護方向，不代表 short。
- 2026-05-09（本輪）：初始 triage 時 `pgrep -af main.py` 顯示仍有 `python3 main.py run`（PID 4981）在跑；`ps` 顯示該進程 15:52:57 啟動。16:09 前後 log 仍有 websocket reconnect、`SERVER_TIME_OFFSET_BLOCKED` 類 time-sync blocked 訊號與大量 signal check，環境不應視為健康穩態。後續使用者回覆已停止，續行輪次也以 `pgrep` 確認無 live process。
- 2026-05-09（本輪）：使用者貼出 live Telegram `POSITION_WITHOUT_STOP_DETECTED` 事件，時間集中在 `2026-05-09T07:53:03Z` 至 `07:53:04Z`（台北時間約 15:53:03-15:53:04）。Telegram `side=SELL` 是保護性平倉方向；程式 `PositionState` 只追蹤 `positionAmt > 0` 的倉位，因此不應解讀成 short。使用者貼出的 symbols：`AGTUSDT`、`ACHUSDT`、`ACEUSDT`、`STORJUSDT`、`SNXUSDT`、`SUSDT`、`AIAUSDT`、`DEEPUSDT`、`FOGOUSDT`、`FIGHTUSDT`、`LITUSDT`。錯誤為 `position_without_stop_on_restore`，reason=`crash_gap_or_manual_cancel`，Telegram 明確標示 manual intervention required。使用者補充線索：2026-05-09 00:46 左右開始，電腦因 disk 空間不足導致問題。
- 2026-05-08（本輪）：解析使用者回報「今天一直出現 `WEBSOCKET_RECONNECTING` / keepalive ping timeout」。結論：**這仍不理想，但比上午那波「reconnect + 巨大 server time offset + -1021」輕一級**。目前貼出的片段只看到單一 websocket chunk（`symbol_count=200`）大約每 10 分鐘 ping timeout 一次，之後有 `WEBSOCKET_CATCHUP_COMPLETED`，暫時**比較像單一連線穩定性差 / 網路抖動 / event loop stall**，而不是整體 private REST 全面失效；但若一整天持續反覆發生，仍不應視為正常穩定狀態，因為每次 reconnect 期間都會有該 chunk 的即時資料空窗，只是系統事後用 REST catch-up 補回 finalized bars。
- 2026-05-08（本輪）：解析使用者重啟後的 live Telegram。結論：**不算正常穩定狀態**。單獨出現 `WEBSOCKET_RECONNECTING` / `WEBSOCKET_RECONNECTED` 在多 chunk websocket 架構下可偶發接受；目前 `data_symbols=529` 且 `WS_MAX_STREAMS_PER_CONNECTION=200`，因此系統會拆成 3 個 websocket chunk，看到多次 `symbol_count=200` 的 reconnect / catch-up 訊息本身不必然是 bug。但這次同時出現 opening handshake timeout、keepalive ping timeout、`SERVER_TIME_OFFSET_BLOCKED`（5.1s→258.5s）、以及最終 `BINANCE_API_BLOCKED code=-1021 path=/fapi/v2/positionRisk`，代表**當下 private REST / 時間同步處於不健康狀態**；至少那段時間的新 entry 前檢查、position refresh 會被擋，不能視為正常。
- 2026-05-08（本輪）：修復 Binance `code=-4015` entry client order id 長度超限。根因是 `pump_system/execution/order_service.py` 原本用 `entry_<symbol>_<13位毫秒>_<6位uuid>`，對 `RESOLVUSDT` 這類 10 碼 symbol 會產生 37 字元，超過 Binance `< 36 chars` 限制。現改為 `entry_<symbol>_<base36 timestamp>_<4位entropy>`，並依 36 字上限動態裁切 symbol 段；`tests/test_order_service_stop.py` 新增長度保護測試。`python3 -m pytest -q` 結果：`44 passed, 1 xfailed`。
- 2026-05-08（本輪）：依使用者要求核對 Copilot / Claude Code 提交狀態。確認目前 `HEAD=236b7e1`，前一個 commit `2c2ec1e feat: Signal Decision Audit + position/order service improvements` 已包含前述 Signal Decision Audit 實作與相關改善；工作區乾淨。核對重點：`pump_system/audit/signal_decision_audit.py` 存在且會寫 append-only JSONL；`pump_system/app.py` 已建立 `SignalDecisionAuditWriter` 並注入 `OrderService`；`pump_system/execution/order_service.py` 每次 `SignalEngine.evaluate()` 後會先寫 `SIGNAL_DECISION`，`triggered=False` 也保留。另確認 `position_state.refresh_symbol()` 已改為 per-symbol `get_position_risk(symbol=...)`，`signal_engine._avg_range_pct()` 已用 valid bar count 作除數，`cleanup_stale_symbols()` 已在 universe 更新後呼叫。
- 2026-05-08（本輪）：參考 Copilot 建議後的優先序判斷：P0/P1 應優先做 audit retention / compression，避免 `data/audit/signal_decisions/` 無上限成長；其次是調整 `SIGNAL_TRIGGERED` 發送時序，目前仍在 `position_state.refresh_symbol()` 與已有倉/上限檢查之前，可能造成「先通知觸發、後跳過」的語意噪音。Layer 2 in-progress kline history 有價值但資料量更大，應等 retention 機制完成後再做。
- 2026-05-08（本輪）：依使用者要求實作第 1 層 `Signal Decision Audit`，用 append-only JSONL 保留每次已評估候選 symbol 的訊號判斷快照。新增 `pump_system/audit/signal_decision_audit.py` 與 `pump_system/audit/__init__.py`；`TradingApplication` 建立 `SignalDecisionAuditWriter`；`OrderService._evaluate_symbol()` 每次 `SignalEngine.evaluate()` 後寫入 `SIGNAL_DECISION`，即使 `triggered=False` 也會保存。另記錄幾個後續 order gate：duplicate bar、已有倉、達持倉上限、symbol info 缺失、order type 缺失、private API unavailable、min legal order、function test 擋單、live disabled simulated、server time unhealthy、margin/leverage/entry failure、entry success。輸出路徑：`data/audit/signal_decisions/signal_decisions_<interval>_<YYYYMMDD>.jsonl`。
- 2026-05-08（本輪）：釐清第 2 層設計風險：若只記 near-miss，確實可能漏掉 `DOGSUSDT` 這類「有被評估但離門檻很遠」的 symbol；若只記 candidate，則 `DOGSUSDT` 在目前設定下會被記到，但未來若被 blacklist / whitelist / excluded 或 universe 異常排除，仍會缺席。建議第 2 層至少記「所有已評估 candidate 的 in-progress update」，near-miss 只作額外標記或延長保留，不作唯一資料來源。
- 2026-05-08（本輪）：依使用者要求巡查 `JTOUSDT` / `DOGSUSDT` 目前無倉位原因。唯讀查 `god_rule.md`、`README.md`、`HANDOFF.md`、`logs/app.log*`、`data/inprogress_15m.csv`、`config.py` 與訊號/下單流程。結論：兩者都有進入 15m 候選池並被評估，未在黑名單/白名單排除，也非 function test 擋單；但 live log 於 03:48 +08 顯示兩者皆 `triggered=False`，原因共同包含 `15m_not_compressed,15m_volume_too_low,15m_push_too_small,15m_not_breakout,already_extended`，因此流程在訊號層返回，沒有進入 `SIGNAL_TRIGGERED` / 持倉刷新 / 下單流程。
- 2026-05-07（本輪）：`SERVER_TIME_OFFSET_BLOCKED` 常態發生根因分析與修復。兩個 commit：1) `f789af4`（Claude Haiku）：`config.py` 預設 `SERVER_TIME_RESYNC_INTERVAL_SECONDS` 300s→60s；`ensure_time_sync()` 加入 `approaching_threshold`（80% 閾值觸發預先 sync）、`consecutive_sync_failures` 連續失敗計數與 try/except 保護。2) `d7a8c0e`（本輪 Copilot）：`_request` retry handler 在 `ensure_time_sync` 後正確重新產生 `timestamp` + `signature`（pop 舊 sig → 重算 timestamp with fresh offset → 重簽）；移除 `BINANCE_API_RETRY` Telegram 通知（降噪，與 HANDOFF 記載對齊）。測試結果：`41 passed, 1 xfailed`。
- 2026-05-07（本輪）：確認 OS 層修復（chrony）需要 sudo 密碼，**已提供指令但未執行**，待使用者手動完成：`sudo apt install chrony && sudo systemctl enable --now chrony && chronyc tracking`。
- 2026-05-07：完成並驗證兩個 live-safe 修復。1) `pump_system/exchange/binance_client.py`：signed request 會在**每次 retry 前重新產生** `timestamp` / `signature`；遇到 Binance `code=-1021` 會先 `ensure_time_sync(force=True)` 後再以短延遲重試；中間態 `BINANCE_API_RETRY` 不再送 Telegram，只保留 final blocked/error 類事件。2) `pump_system/notify/telegram_notifier.py`：加入全域節流（最小發送間隔、每分鐘上限）、Telegram 429 `retry_after` flood-wait、低優先級訊息丟棄、同 symbol 同 bar 的 `SIGNAL_TRIGGERED` 去重，以及 flood-wait 結束後自動補送 deferred 高優先級事件。另在 `pump_system/execution/order_service.py` 的 `SIGNAL_TRIGGERED` 詳情新增 `bar_key`，讓 notifier 去重真正生效。新增/更新測試：`tests/test_binance_client.py`、`tests/test_telegram_notifier.py`、`tests/test_order_service_signal_notifications.py`；`python3 -m pytest -q` 結果為 `41 passed, 1 xfailed`。
- 2026-05-07：依使用者要求驗證 Claude 已處理的 VM 時間同步問題。`timedatectl status` 顯示 `System clock synchronized: yes`、`NTP service: active`；`systemd-timesyncd` 於 00:17:30 +08 重啟，00:18:01 +08 已聯到 `ntp.ubuntu.com` 完成 initial clock synchronization。執行 `python3 time_sync_diagnostic.py --repeat 3`，Binance offset 三次為 `+236 ms / +70 ms / +52 ms`，平均 `+119 ms`，低於 `threshold_ms=5000`；結論：VM / Binance server time offset 已恢復正常範圍。
- 2026-05-07：解析使用者貼上的 live Telegram 訊息。結論：`HOMEUSDT` 15m 訊號條件通過並重複送出 `SIGNAL_TRIGGERED`，但 private/signed Binance 流程因 `SERVER_TIME_OFFSET_BLOCKED` 被擋；`offset_ms=-13776` 超過 `threshold_ms=5000`，後續 `position_state.refresh_symbol()` / signed request 丟出 `timestamp_unhealthy`，形成 `SYMBOL_EVALUATION_BLOCKED`。貼文片段未出現 `ENTRY_ORDER_SUCCESS` 或 `STOP_ORDER_SUCCESS`，因此不能判定有下單成功。
- 2026-05-04：依 `god_rule.md` RULE 01 將過長的 `HANDOFF.md` 瘦身為熱區版本。
- 整理前 `HANDOFF.md` 完整原文已先附加到 `HANDOFF_ARCHIVE.md` 末尾；原始行數 822，SHA256 已記錄於 archive 搬遷段。
- 2026-05-04 那輪只修改 `HANDOFF.md` 與 `HANDOFF_ARCHIVE.md`；未改程式碼、未改 `.env`、未跑測試、未連 API、未查帳戶、未查/寫 DB。

### 進行中 / 尚未完成
- [TODO] audit retention/compression 程式已修好且測試通過，但尚未進入 live process；若要生效，需使用者明確授權後再啟動/重啟 bot。下次啟動後背景維護會依設定處理跨日 JSONL；本輪仍未動既有 2026-05-08 / 2026-05-09 audit 檔。
- [TODO] 2026-05-09 `POSITION_WITHOUT_STOP_DETECTED` 是 live risk 事件；目前已確認 Binance 現況 `missing_protective_stop=NONE`，但事故根因仍需持續從 disk full / crash gap / restore 時 fallback state 與 native stop watchlist 斷層方向追查。未經使用者更明確要求，不可代替使用者掛 stop、平倉、補單、修改 `.env` 或重啟 live bot。
- [TODO] 上述 notifier / Binance retry 修復目前只在程式碼與測試中完成；若要進入 live process，仍需由使用者明確授權後重啟載入新程式，不能沿用舊進程視為已生效。
- [TODO] 使用者表示不需補單，會自行重啟 bot；重啟後只需觀察 Telegram 是否不再出現 `SERVER_TIME_OFFSET_BLOCKED` / `timestamp_unhealthy`，並確認後續 trade 事件是否正常。
- [TODO] 若要讓 2026-05-02 的槓桿 `-4424` fallback 修正進入 live process，必須先取得明確授權並確認目前是否已有 live bot 在跑；不可自行重啟或開第二個 `main.py run`。
- [TODO] `tests/test_algo_fill_regression.py::test_reconcile_triggered_via_algo_history_fallback` 仍是 P1 未完項；若要處理，需釐清 Binance historical algo endpoint，再在 `BinanceClient` / `OrderService` 接入。
- [TODO] Aggressive / Bucket C 研究目前只能進 shadow / prototype signal spec 設計審查；不可直接部署到主交易邏輯。
- [TODO] `SIGNAL_15M_*` 雖已獨立，但仍需持續觀察與回測校準；不可因單一 near-miss 直接放寬正式盤主策略門檻。

### 關鍵檔案清單
| 檔案路徑 | 用途說明 |
|---|---|
| `HANDOFF.md` | 熱區：目前狀態、風險、下一步 |
| `HANDOFF_ARCHIVE.md` | 冷區：完整歷史紀錄，只增不減 |
| `README.md` | 主線規格、runbook、資料流與風險說明 |
| `tools/binance_usdm_income_readonly.py` | v1 Binance USD-M Futures income read-only 查詢工具；預設 dry-run |
| `tools/binance_usdm_income_readonly_GUIDE.md` | v1 工具簡短使用說明 |
| `config.py` | `.env` 載入、`STRATEGY_INTERVAL`、訊號/止損/下單設定 |
| `main.py` | CLI 入口：`validate` / `backfill` / `run` |
| `pump_system/app.py` | 主流程組裝、背景任務、WebSocket / DB / execution 串接 |
| `pump_system/audit/signal_decision_audit.py` | append-only 訊號決策 audit JSONL writer |
| `pump_system/audit/jsonl_parquet.py` | signal decision audit 跨日壓縮與 retention 維護器 |
| `pump_system/strategy/signal_engine.py` | 主策略訊號條件與 fail reasons |
| `pump_system/execution/order_service.py` | 下單、槓桿設定、native stop、fallback 觸發入口 |
| `pump_system/fallback_stop/manager.py` | fallback stop 觸發與市價平倉入口 |
| `pump_system/utils/client_order_id.py` | Binance-safe client order id builder，處理非 ASCII symbol |
| `pump_system/notify/telegram_notifier.py` | Telegram flood-wait / 節流 / 高低優先級 / signal 去重 |
| `pump_system/execution/sizing.py` | quantity / notional / leverage sizing |
| `pump_system/exchange/binance_client.py` | Binance public/private wrapper；P1 algo history 會動到這裡 |
| `tests/test_telegram_notifier.py` | notifier flood-wait / 去重測試 |
| `tests/test_binance_client.py` | signed retry 重簽 / -1021 測試 |
| `tests/test_order_service_signal_notifications.py` | `SIGNAL_TRIGGERED` 是否附帶 `bar_key` 測試 |
| `pump_system/db/repository.py` | finalized K 線 DB 寫入/讀取；歷史資料不可覆蓋 |
| `reports/aggressive_backtest_audit_20260430.py` | aggressive audit / live-safe audit 腳本 |
| `reports/aggressive_backtest_audit_live_safe_20260502.md` | 修正版 live-safe audit 報告 |
| `reports/aggressive_backtest_live_safe_stability_20260502.md` | Bucket C 穩健性補充報告 |

### 注意事項 / 已知風險
- [RISK] 使用者表示 PID 4981 已停止，且本輪 `pgrep` 已確認沒有 `python3 main.py run`；但任何重啟前仍需重新確認沒有第二個 live process，避免雙開。
- [RISK] 15:53 log 顯示 31 個 positions 在 restore 時沒有 native stop / fallback stop；本輪稍後只讀查 Binance 已確認目前 43 個 open positions 都有 1 張 protective `STOP_MARKET SELL` conditional algo stop。此結果只代表查詢當下現況，不代表 disk full / crash gap 根因已完全解除。
- [RISK] `data/audit/signal_decisions/` 單日檔案已達 GB 級；程式已加入跨日 gzip/retention，但既有 2026-05-08 / 2026-05-09 JSONL 本輪依使用者指示未刪除、未壓縮、未搬移。若未啟動新版程式或未手動釋放空間，磁碟仍可能再次打滿。
- [RISK] `POSITION_WITHOUT_STOP_DETECTED` 表示程式 restore 時發現帳上存在倉位，但沒有對應 native stop / fallback stop 保護；對交易系統屬 P0/P1 live safety 事件。此狀態下不應假設 bot 會自動補救，Telegram 已要求 manual intervention。
- [RISK] 本輪已修正 restore 判定邏輯，但目前正在跑的 `python3 main.py run` 還是舊進程，新的條件要等下次 restart 才會生效；在此之前 Telegram 仍可能看到同一批啟動期告警。
- [RISK] 2026-05-09 live run 已證實舊版 restore 判定會忽略不以 `stop_` 開頭的 `clientAlgoId`。這就是本輪修復前的 false negative 根因；修正已寫入程式，但當前正在跑的舊進程仍未載入。
- [RISK] 最新 Telegram 顯示 `SERVER_TIME_OFFSET_BLOCKED` 反覆出現且 offset 已升到五位數甚至六位數 ms，並伴隨 `BINANCE_API_BLOCKED`。這表示 live 已不只是一開始的 restore 誤判，還有 signer/time-sync 與 private REST 健康度問題，會影響 position refresh、openAlgoOrders 查詢與後續風險判定。
- [RISK] 使用者提供的 disk 空間不足線索可能導致 log/audit/state CSV 寫入失敗、process crash、restore gap、或 stop 狀態無法可靠落盤；仍需查證本地 `logs/`、`data/fallback_stop_state.csv`、`data/inprogress_*.csv` 與磁碟狀態後才能定論。
- [RISK] 2026-05-08 下午新增觀察：即使沒有再次貼出 `SERVER_TIME_OFFSET_BLOCKED` / `-1021`，單一 chunk 若持續每數分鐘到十幾分鐘就 `keepalive ping timeout`，仍表示 websocket 連線品質不穩。系統會 catch up finalized bars，但 reconnect 空窗內的即時 in-progress 更新可能不連續，對依賴當下流式狀態的判斷不是理想狀態。
- [RISK] 2026-05-08 重啟後雖然 websocket 最後有 `WEBSOCKET_RECONNECTED` / `WEBSOCKET_CATCHUP_COMPLETED`，但同一時段仍有 handshake timeout、ping timeout、巨大正 offset、以及 `/fapi/v2/positionRisk` 的 `-1021`。這比較像 VM / 網路 stall 或時間同步量測受高 RTT 影響，而不是單純穩定恢復；若後續仍持續出現，就不應視為 live-safe。
- [RISK] `SignalDecisionAuditWriter` 仍會對每次已評估 candidate 訊號判斷寫一行 JSONL；retention/compression 只處理跨日檔，今天正在寫入的 daily JSONL 仍可能快速增長。
- [RISK] 第 2 層若只做 near-miss symbols 會漏掉非 near-miss 的 DOGS 類案例；不建議 near-miss 作唯一記錄範圍。
- [RISK] 2026-05-08 JTO/DOGS 巡查未連 private/account/order API、未查帳戶、未查/寫 DB；「無倉位原因」是基於本地 live log 與流程碼判讀：系統未產生 JTO/DOGS 入場訊號，因此未嘗試開倉。
- [RISK] `SIGNAL_TRIGGERED` 仍在 `position_state.refresh_symbol()` 之前送出；雖然 notifier 現已用 `bar_key` 去重同 symbol 同 bar 的重複 Telegram，但若 live process 尚未重啟載入新版程式，舊進程仍可能重複推播。
- [RISK] `TIME_SYNC_TROUBLESHOOT.md` 對 offset 正負號的文字說明疑似與程式實作不一致；程式實作為 `server_time - local_midpoint`，因此 `offset_ms=-13776` 代表本機 midpoint 約快於 Binance server 13.776 秒。未經使用者要求，本輪不修改文件。
- [RISK] 本專案是交易系統；未經明確要求，不可修改交易邏輯、切換 live/testnet、建立憑證、修改 `.env`、連交易所帳戶/API、查帳戶或寫 DB。
- [RISK] `HANDOFF_ARCHIVE.md` 是永久冷區，只可附加不可刪改；若未來再瘦身，仍需先完整附加原文再移除熱區段落。
- [RISK] 2026-05-02 記錄顯示 live process 可能尚未載入 `-4424` fallback 整合 commit；下次處理 live 前必須重新確認，不可沿用舊 PID 當現況事實。
- [RISK] 目前主線方向不是把正式策略從 15m 改回 3m；3m 在 aggressive audit 中主要是 Layer 2 重建粒度。
- [RISK] Aggressive 回測修正後 Bucket C conservative 仍有正 expectancy，但 96bar median 為負，且 2026-04 stop 行為惡化；只可作研究/設計輸入，不可直接部署。
- [RISK] `return_pct_min` 曾於 2026-04-29 調到 `0.017`；若任務依賴實際執行門檻，需讀當前程式與 `.env`（不可輸出 secrets）重新確認。
- [SKIP] 本次 handoff 整理與資料流重用、PostgreSQL 寫入保護、策略績效驗證無直接關係；本輪沒有碰資料流、DB 或交易邏輯。

### 下一步建議
1. 重啟 live bot 前先確認工作區變更是否要一起帶入，並再次確認沒有既有 `main.py run` 進程；本輪未替使用者重啟。
2. 若要立刻釋放磁碟空間，需使用者另行明確授權壓縮/搬移既有 `data/audit/signal_decisions/*.jsonl`；本輪依「先保留」未動既有 audit 檔。
3. 重啟後觀察 logs/Telegram：確認不再出現 `POSITION_WITHOUT_STOP_DETECTED`、`SERVER_TIME_OFFSET_BLOCKED`、長時間 websocket reconnect，並確認 audit maintenance 有寫入 `signal audit maintenance complete` log。
1. 若後續只剩偶發單次 reconnect 且不再伴隨 `SERVER_TIME_OFFSET_BLOCKED` / `-1021`，可先觀察；但如果像今天這樣整天反覆 `keepalive ping timeout`，即使 catch-up 有完成，也應視為基礎設施或 websocket 穩定性問題，不算正常穩態。
1. 若要讓本輪修復進入 live，先確認目前是否已有 `main.py run` 進程，再由使用者明確授權後重啟載入新版程式；本輪未代替使用者重啟。
2. 重啟後優先觀察 Telegram 是否不再長時間 429、`SIGNAL_TRIGGERED` 是否不再同 bar 連發、以及 `code=-1021` 是否消失或顯著下降。
3. 若下一個任務是 P1 algo history fallback，先只讀核對 Binance endpoint 與現有 xfail 測試，再做最小改動與測試。
4. 若下一個任務是 aggressive 分支，先做 shadow signal spec / 紙上交易設計，不要改主交易流程。
5. 若只是查歷史脈絡，優先在 `HANDOFF_ARCHIVE.md` 查 2026-05-04 搬遷段；不要把 `HANDOFF.md` 再膨脹成完整歷史。

### 關鍵決策紀錄
- 2026-05-09：使用者本輪已授權 read-only 查 Binance 帳戶現況；本輪只查 GET 類 private endpoints，未送 order/cancel/leverage/margin POST，未修改 `.env`。
- 2026-05-09：audit retention/compression 採背景維護與 gzip 預設；只處理跨日 JSONL，今天檔案不處理，舊 JSONL 必須成功封存後才可刪除。使用者指定「先保留」既有 audit 檔，因此本輪不手動壓縮/刪除/搬移 2026-05-08 / 2026-05-09 JSONL。
- 2026-05-09：本輪確認 `side=SELL` 是保護性平倉方向；不得把 Telegram `SELL` 解讀為 short。本輪後續已獲授權查 Binance 現況，查詢當下全部 43 個 open positions 均有 protective stop。
- 2026-05-09：`POSITION_WITHOUT_STOP_DETECTED` 依 Telegram 內容與專案領域規則先視為需要人工介入的 live safety 事件；初始輪次只允許文件記錄與唯讀分析。後續使用者授權後只做 read-only 帳戶查詢，仍不自動補 stop / 平倉 / 重啟。
- 2026-05-07：依使用者授權繼續處理「5 / 6」兩個工程修復，範圍限定為 notifier 降噪/429 處理與 Binance signed retry 重簽；不改 `.env`、不重啟 live bot、不碰交易策略。
- 2026-05-07：VM 時間同步問題已驗證修復；使用者明確表示「不用補單，我自行重啟 bot」。後續不可代替使用者補單或重啟，除非取得新的明確授權。
- 2026-05-07 live Telegram 解析採程式實作為準：`SERVER_TIME_OFFSET_BLOCKED` 是 signed request 前的安全擋板；看到 `SIGNAL_TRIGGERED` 不等於已送單，需以 `ENTRY_ORDER_SUCCESS` / `STOP_ORDER_SUCCESS` 等 trade 事件判斷下單與止損是否成功。
- `god_rule.md v1.5.0` 優先於所有下位文件；有衝突時先列衝突，再以 `god_rule.md` 為準。
- `HANDOFF.md` 保持熱區；完整歷史保存在 `HANDOFF_ARCHIVE.md`。
- 正式主線支援 `STRATEGY_INTERVAL=3m|15m`，目前 README/交接脈絡以 15m 為主；切換需重啟且不得自行改 `.env`。
- finalized bars 才寫 PostgreSQL；in-progress bars 留 staging / `data/inprogress_<STRATEGY_INTERVAL>.csv`。
- 原生止損正式路徑固定為 Binance `/fapi/v1/algoOrder` 的 `STOP_MARKET`；fallback stop 只在 native stop 失敗後監控並市價平倉，不是重新掛 stop。
- Function test mode 開啟時，只有 `FUNCTION_TEST_SYMBOL` 可真實下單；其他 symbol 只評估與通知。
- DAMUSDT / SOLVUSDT 等歷史案例目前屬策略取捨與回測研究脈絡，不是已確認的候選池漏單 bug。

### 資源回報
- 2026-05-11 本輪類型：先提交既有變更，再修復 Binance `newClientOrderId/clientAlgoId` 非 ASCII bug。驗證方式：`df -h` 確認空間已釋出；`git add -A && git commit -m "chore: save current trading bot updates"` 成功產生 `8c5cda7`；修改 `pump_system/utils/client_order_id.py`、`pump_system/execution/order_service.py`、`pump_system/fallback_stop/manager.py`、`tests/test_order_service_stop.py`、`tests/test_fallback_stop_manager.py`、`HANDOFF.md`；跑 py_compile、目標 pytest、全 pytest。未改 `.env`、未啟動/停止/重啟 bot、未連 Binance、未查帳戶、未查/寫 DB、未改交易訊號條件、未改 sizing。
- 2026-05-10 本輪類型：v1 Binance USD-M Futures income read-only 工具實作。驗證方式：讀 `AGENTS.md`、`god_rule.md`、`README.md`、`HANDOFF.md`；記錄 `git HEAD=236b7e1`；新增 `tools/binance_usdm_income_readonly.py`、`tools/binance_usdm_income_readonly_GUIDE.md`；修改 `.gitignore` 追加 `tools/outputs/`；更新 `HANDOFF.md` 履行 RULE 01；跑 `python3 -m py_compile tools/binance_usdm_income_readonly.py`、`python3 tools/binance_usdm_income_readonly.py --help`、fake env dry-run。未讀真實 `.env`、未改 `.env`、未發 Binance request、未查帳戶、未查/寫 DB、未啟動/停止/重啟 bot、未改交易邏輯、未執行 `main.py run/backfill/validate`。
- 2026-05-10 本輪類型：Claude Code prompt review 結果再審。驗證方式：讀 `god_rule.md`、`README.md`、`HANDOFF.md`、`AGENTS.md`，補核 Binance 官方 `/fapi/v1/income` 與 API-Agent futures rebate 文件；只修改 `HANDOFF.md` 以履行 RULE 01。未讀 `.env`、未改 `.env`、未建立工具檔、未啟動/重啟 bot、未連 Binance 帳戶/API、未查帳戶、未查/寫 DB、未改交易邏輯、未執行被審查 prompt。
- 2026-05-10 本輪類型：修正版完整 prompt 交付。驗證方式：沿用前序 context 與 Binance 官方 `/fapi/v1/income` 分頁核對結果，整理成完整最終 prompt；只修改 `HANDOFF.md`。未讀 `.env`、未建立工具檔、未建立 key profile 設定檔、未啟動/重啟 bot、未連 Binance 帳戶/API、未查帳戶、未查/寫 DB、未執行 prompt 內容。
- 2026-05-10 本輪類型：Claude 最終版 prompt 結果核對。驗證方式：讀使用者貼出的 Claude transcript，核對 Binance 官方 `/fapi/v1/income` 分頁/時間行為；只修改 `HANDOFF.md`。未讀 `.env`、未建立工具檔、未建立 key profile 設定檔、未啟動/重啟 bot、未連 Binance 帳戶/API、未查帳戶、未查/寫 DB、未執行 prompt 內容。
- 2026-05-10 本輪類型：Claude Code Opus 4.7 接手做整體 context 重審 + 最終 prompt 重新交付。驗證方式：讀 `god_rule.md`、`README.md`、`HANDOFF.md`、`AGENTS.md`、`config.py`、`.env.example`、`.gitignore`、`requirements.txt`、`pump_system/exchange/binance_client.py` 開頭 120 行；比對 Binance `/fapi/v1/income` weight 與分頁行為；只修改 `HANDOFF.md`。未讀 `.env`、未建立工具檔、未建立 key profile 設定檔、未啟動/重啟 bot、未連 Binance 帳戶/API、未查帳戶、未查/寫 DB、未執行 prompt 內容。
- 2026-05-10 本輪類型：最終完整 prompt 交付。驗證方式：沿用前序 prompt review 結論與使用者裁決，整理成最終命令式執行規格；只修改 `HANDOFF.md`。未讀 `.env`、未建立 key profile 設定檔、未建立工具、未啟動/重啟 bot、未連 Binance 帳戶/API、未查帳戶、未查/寫 DB、未執行 prompt 內容。
- 2026-05-10 本輪類型：10 點補強段落的純 prompt review。驗證方式：讀 `god_rule.md`、`README.md`、`HANDOFF.md`、`AGENTS.md`；只修改 `HANDOFF.md` 以履行 RULE 01。未讀 `.env`、未建立 key profile 設定檔、未建立工具、未啟動/重啟 bot、未連 Binance 帳戶/API、未查帳戶、未查/寫 DB、未執行 prompt 內容。
- 2026-05-10 本輪類型：prompt 補強決策整理。驗證方式：依前序 prompt review 結論與使用者逐項決策整理可插入 prompt 的修正段落；只修改 `HANDOFF.md`。未讀 `.env`、未建立 key profile 設定檔、未建立工具、未啟動/重啟 bot、未連 Binance 帳戶/API、未查帳戶、未查/寫 DB、未執行 prompt 內容。
- 2026-05-10 本輪類型：新版 implementation prompt 的純 review。驗證方式：讀 `god_rule.md`、`README.md`、`HANDOFF.md`、`AGENTS.md`、`config.py` 相關 base URL 片段，並核對 Binance 官方 `/fapi/v1/income` 文件。只修改 `HANDOFF.md` 以履行 RULE 01。未讀 `.env`、未改 `.env`、未建立工具、未啟動/重啟 bot、未連 Binance 帳戶/API、未查帳戶、未查/寫 DB、未執行 prompt 內容。
- 2026-05-10 本輪類型：補強版 prompt 交付。驗證方式：沿用前序已讀專案脈絡與官方 endpoint 核對結果，依使用者補充將 `.env.example` 排除為執行前提；只修改 `HANDOFF.md`。未讀 `.env`、未改 `.env`、未啟動/重啟 bot、未連 Binance 帳戶/API、未查帳戶、未查/寫 DB、未改交易邏輯、未執行 prompt 內容。
- 2026-05-10 本輪類型：兩段外部 agent QA 的 prompt/meta-review 分析。驗證方式：讀 `god_rule.md`、`README.md`、`HANDOFF.md`、`AGENTS.md`、`.env.example`、`pump_system/exchange/binance_client.py` 片段，並核對 Binance 官方 `/fapi/v1/income`、`/fapi/v1/userTrades` 文件。只修改 `HANDOFF.md` 以履行 RULE 01。未讀 `.env`、未改 `.env`、未重啟 bot、未連 Binance 帳戶/API、未查帳戶、未查/寫 DB、未改交易邏輯、未執行被審查 prompt。
- 2026-05-09 本輪續行類型：live safety 後續處理、Binance read-only account check、audit retention/compression 程式修補。
- 2026-05-09 驗證方式：讀 `god_rule.md`、`README.md`、`HANDOFF.md`、相關程式與測試；確認 `git HEAD=236b7e1`；`pgrep` 確認無 `python3 main.py run`；用現有 `.env` 只讀查 `/fapi/v2/positionRisk`、`/fapi/v1/openOrders`、`/fapi/v1/openAlgoOrders`；語法檢查通過；`python3 -m pytest -q tests/test_signal_decision_audit.py tests/test_app_heartbeat.py` 得 `12 passed`；`python3 -m pytest -q` 得 `48 passed, 1 xfailed`。修改 `.gitignore`、`.env.example`、`README.md`、`config.py`、`pump_system/app.py`、`pump_system/audit/signal_decision_audit.py`、`pump_system/audit/jsonl_parquet.py`、`tests/test_app_heartbeat.py`、`tests/test_signal_decision_audit.py`、`HANDOFF.md`。未改 `.env`、未重啟 bot、未送 order/cancel/leverage/margin POST、未查/寫 DB、未改交易訊號門檻或下單判斷、未刪除/壓縮/搬移既有 audit 檔。
- 2026-05-09 本輪類型：`POSITION_WITHOUT_STOP_DETECTED` 本機唯讀 triage、code path 判讀、磁碟來源確認與 handoff 更新。
- 2026-05-09 驗證方式：讀 `logs/app.log*`、`data/fallback_stop_state.csv`、`pump_system/execution/order_service.py`、`pump_system/app.py`、`pump_system/state/position_state.py`；執行唯讀 `df` / `du` / `ls` / `stat` / `ps` / `pgrep` / `rg` / `sed`。只修改 `HANDOFF.md`。未改 `.env`、未重啟/停止 bot、未連 private/account/order API、未查帳戶、未查/寫 DB、未改交易邏輯、未刪除或壓縮任何檔案。
- 2026-05-09 本輪類型：`POSITION_WITHOUT_STOP_DETECTED` live Telegram 事故初始記錄與 handoff 更新。
- 2026-05-09 驗證方式：讀 `god_rule.md`、`README.md`、`HANDOFF.md`；記錄 `git HEAD=236b7e1`；只修改 `HANDOFF.md`。未改 `.env`、未重啟 bot、未連 private/account/order API、未查帳戶、未查/寫 DB、未改交易邏輯。
- 2026-05-08 本輪類型：持續 websocket reconnect live 訊息判讀與 handoff 更新。
- 2026-05-08 驗證方式：唯讀讀 `HANDOFF.md`、`pump_system/market_data/websocket_manager.py`、`pump_system/app.py`、`README.md`；未改 `.env`、未重啟 bot、未跑測試、未連 private/account/order API、未查帳戶、未查/寫 DB、未改交易邏輯。
- 2026-05-08 本輪類型：重啟後 websocket / server-time live 訊息判讀與 handoff 更新。
- 2026-05-08 驗證方式：唯讀讀 `HANDOFF.md`、`README.md`、`pump_system/market_data/websocket_manager.py`、`pump_system/app.py`、`pump_system/sync/time_sync_manager.py`、`config.py`、`pump_system/exchange/binance_client.py`；未改 `.env`、未重啟 bot、未跑測試、未連 private/account/order API、未查帳戶、未查/寫 DB、未改交易邏輯。
- 2026-05-08 本輪類型：Binance entry order client id 長度修復與 handoff 更新。
- 2026-05-08 驗證方式：先跑 baseline `python3 -m pytest -q` 得 `43 passed, 1 xfailed`；修改 `pump_system/execution/order_service.py`、`tests/test_order_service_stop.py` 後再跑 `python3 -m pytest -q` 得 `44 passed, 1 xfailed`。未改 `.env`、未重啟 bot、未連 private/account/order API、未查帳戶、未查/寫 DB、未改交易邏輯。
- 2026-05-08 本輪類型：Signal Decision Audit 實作、測試與 handoff 更新。
- 2026-05-08 驗證方式：`python3 -m py_compile pump_system/audit/signal_decision_audit.py pump_system/app.py pump_system/execution/order_service.py tests/test_signal_decision_audit.py tests/test_order_service_signal_notifications.py`；`python3 -m pytest -q tests/test_signal_decision_audit.py tests/test_order_service_signal_notifications.py`；`python3 -m pytest -q tests/test_order_service_stop.py tests/test_signal_decision_audit.py tests/test_order_service_signal_notifications.py`；`python3 -m pytest -q` 結果 `43 passed, 1 xfailed`。未改 `.env`、未重啟 bot、未連 private/account/order API、未查帳戶、未查/寫 DB、未改訊號門檻或下單判斷。
- 2026-05-08 本輪類型：`JTOUSDT` / `DOGSUSDT` 無倉位原因巡查與 handoff 更新。
- 2026-05-08 驗證方式：讀 `god_rule.md`、`README.md`、`HANDOFF.md`；查 `logs/app.log*`、`data/inprogress_15m.csv`、`config.py`、`pump_system/execution/order_service.py`、`pump_system/strategy/signal_engine.py`、`pump_system/exchange/symbol_registry.py`；用 `python3` 只輸出非機密設定。未改 `.env`、未重啟 bot、未連 private/account/order API、未查帳戶、未查/寫 DB、未改交易邏輯。
- 2026-05-07 本輪類型：Telegram flood-wait / Binance retry 工程修復、測試與 handoff 更新。
- 2026-05-07 驗證方式：`python3 -m pytest -q tests/test_telegram_notifier.py tests/test_binance_client.py tests/test_order_service_signal_notifications.py tests/test_order_service_stop.py` 與 `python3 -m pytest -q`；未改 `.env`、未重啟 bot、未連 private/account/order API、未查帳戶、未查/寫 DB。
- 2026-05-07 本輪類型：VM / Binance time offset 驗證與 handoff 更新。
- 2026-05-07 驗證方式：`timedatectl status`、`systemctl status systemd-timesyncd`、`python3 time_sync_diagnostic.py --repeat 3`；未改程式碼、未改 `.env`、未重啟 bot、未查帳戶、未查/寫 DB。
- 2026-05-07 本輪類型：live Telegram 訊息解析 / 程式碼唯讀核對 / handoff 更新。
- 2026-05-07 驗證方式：讀 `god_rule.md`、`README.md`、`HANDOFF.md` 與最小相關程式碼；未跑測試、未連 API、未查帳戶、未查/寫 DB；只修改 `HANDOFF.md`。
- 本輪類型：文件整理 / handoff 歸檔。
- 驗證方式：唯讀檢查行數、archive marker / hash、檔案差異；未跑測試。
